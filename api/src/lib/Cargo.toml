[package]
name = "lib"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2.0.15"

axum = { version = "0.8.4", default-features = false, features = ["json"] }

serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.143"
