[package]
name = "lib"
version = "0.1.0"
edition = "2021"

[dependencies]
askama = { version = "0.12.1", features = ["with-axum"] }
askama_axum = { version = "0.4.0" }
axum = { version = "0.8.1", optional = true }

[features]
default = []
axum = ["dep:axum"]
